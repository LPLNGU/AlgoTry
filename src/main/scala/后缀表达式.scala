import java.util
import scala.collection.mutable

object 后缀表达式 {
//  def evalRPN(tokens: Array[String]): Int = {
//    var stack = mutable.Stack()
//    val handleChar = (char: Char, stack: mutable.Stack[Char]) => {
//      if(char > '0' && char < '9')
//        stack
//    }
//    tokens.foreach()
//
//  }
}
